L_test = [1, 2, 3, 4]
T_test = (1, 2, 3, 4)
S_test = {1, 2, 3, 4}
T_test.reverse()
print("append(), extend(), insert(), remove(), pop(), reverse, nie działają dla set")
print("append(), extend(), insert(), remove(), pop(), reverse, nie działają dla tuple")